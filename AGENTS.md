# Agent Instructions

Read `CLAUDE.md` and `README.md` before changes.

- Preserve the native SwiftUI + SpriteKit architecture.
- Run simulator tests after gameplay, purchase, or project-file changes.
- Treat `project.yml` as the source of truth; regenerate the Xcode project with XcodeGen.
- Never commit `Config/Secrets.xcconfig` or any secret credential.
- App Store claims must match implemented behavior.
- Keep the full core game free; Commander Pack is cosmetic.
- Ask before deleting progress, assets, signing configuration, or App Store records.
